pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
gummidir = pkgdatadir

# Install snippet files
install_data(
  'snippets.cfg',
  'snippets.lang',
  install_dir: join_paths(gummidir, 'snippets')
)