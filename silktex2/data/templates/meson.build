# Templates installation

template_files = [
  'article.tex',
  'beamer.tex',
  'book.tex',
  'letter.tex',
  'report.tex'
]

install_data(
  template_files,
  install_dir: join_paths(get_option('datadir'), 'silktex', 'templates')
)
